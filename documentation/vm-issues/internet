
test with:

1. enabling it in the settings (usually nothing to see in the GUI)


2. 
sudo dhclient
sudo ip link set ens33 up

3. If "Temporary failure in name resolution"

run 
sudo systemctl enable --now systemd-resolved
sudo systemctl restart systemd-resolved