not sure exactly which steps solved it:

sudo apt install -y open-vm-tools open-vm-tools-desktop


pkill -f vmtoolsd
pkill -f vmware-user


export DISPLAY=:0
export XAUTHORITY=$HOME/.Xauthority
/usr/bin/vmware-user &