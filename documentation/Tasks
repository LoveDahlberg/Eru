Add clang formatter file
    Fix so that "type *name" becomes "type* name"

Add ninja to project

Refactor Parser.cpp into separate files. Functions that don't generate a type should be in control files, functions that parse types should be 
separate.

Implement smart points for things that are not passed directly to llvm (now everything leaks). Think about how to do with the
llvm passed pointers.

Trim includes in files.

Go through TODO and add tasks

Create a "ExpectAndEat" function for Parser.cpp instead of checking type and then skipping it everywhere.