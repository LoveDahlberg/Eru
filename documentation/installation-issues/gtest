Build from source:

git clone https://github.com/google/googletest.git
cd googletest
mkdir build
cd build
# Use the same compiler and flags as your project
CXX=clang++ CXXFLAGS="-stdlib=libstdc++" cmake .. -DCMAKE_INSTALL_PREFIX=/usr/local/gtest-libstdc

make
sudo make install