
#pragma once

// Enivronment variable overrides

/// The path to the tmp directory, where working files are placed.
constexpr auto tmpDirectoryPath = "ERU_TMP_PATH";

/// The path to the Ainur standard library.
constexpr auto ainurPath = "ERU_AINUR_PATH";